┌──────────────────────────────┐
│        网文生成系统           │
├──────────────────────────────┤
│ Prompt 层（Dify）             │
│  ├─ 写作指令                  │
│  ├─ 风格约束                  │
│  └─ 世界观规则                │
├──────────────────────────────┤
│ RAG 层（双库）                │
│  ├─ RAG_A：名著参考库         │ ← 风格 / 技法 / 语言节奏
│  └─ RAG_B：作品记忆库         │ ← 人物 / 剧情 / 设定
├──────────────────────────────┤
│ 检索策略层                    │
│  ├─ 召回（向量）              │
│  ├─ 重排（规则 / LLM）        │
│  └─ 拼接（上下文构建）        │
├──────────────────────────────┤
│ 存储层（Qdrant）              │
└──────────────────────────────┘


原始章节
↓
解析章节结构（章号 / 标题 / 段落）
↓
构造「写作片段 chunk」（滑窗）
↓
生成写作元数据（节奏 / 情绪 / 类型）
↓
向量化（面向“怎么写”）
↓
写入 collection/documents/
↓
更新 meta.json
